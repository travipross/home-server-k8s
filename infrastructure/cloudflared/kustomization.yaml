apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: cloudflared
metadata:
  labels:
    app: cloudflared
resources:
  - terraform-tunnel-token-secret.enc.yaml
  - terraform-tunnel-deployment.yaml
