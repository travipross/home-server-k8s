apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: transmission-watch
  labels:
    app: transmission
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs-nas
