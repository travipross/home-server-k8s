apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zwave-js-ui-config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn-retain
  volumeName: zwave-js-ui-config  # originally dynamically created