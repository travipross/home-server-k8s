apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: esphome-httproute
spec:
  parentRefs:
    - name: traefik-gateway
      namespace: kube-system
  hostnames:
    - esphome.travisprosser.ca
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: esphome
          port: 80
