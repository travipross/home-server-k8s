apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: esphome-config-longhorn-pvc
  labels:
    app: esphome
  namespace: iot
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn
