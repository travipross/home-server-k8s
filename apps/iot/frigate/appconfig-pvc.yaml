apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: frigate-appconfig
  labels:
    app: frigate
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn
