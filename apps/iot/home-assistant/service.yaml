apiVersion: v1
kind: Service
metadata:
  name: home-assistant
  namespace: iot
spec:
  type: ClusterIP
  selector:
    app: home-assistant
  ports:
    - name: http
      port: 8123
      targetPort: 8123
