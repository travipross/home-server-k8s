apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lubelogger-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn-retain
  volumeName: lubelogger-data # originally dynamically created
